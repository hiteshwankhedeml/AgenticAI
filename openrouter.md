# OpenRouter

* Models by all providers
* There are free models as well
* While calling the LLM we can specify auto ⇒ it will route to paid model
* We can also specify name of free models
* For free limits, there is limit based on hits per minute or day etc
* We can also use openrouter/free ⇒ This is for free models, it has 23 models
* Openrouter gives multiple closed source models also for free



**AI Models:**

* **Closed Source:** Weignts are not known
* **Open Source:**&#x20;
  * Weights are known, can be hosted on our infra
  * We can use ollama to run them locally
  * Every model will be having different number of parameters and will be needing different resources
