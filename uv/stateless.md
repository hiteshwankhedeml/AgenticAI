# Stateless

* LLM calls are stateless
* All the previous messages also  needs to be send to LLM so that it gets the context and then reply
* So for long chats, more and more context will get used
* Every LLM model has context limit

