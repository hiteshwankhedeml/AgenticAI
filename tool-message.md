# Tool Message

* Type will be AI message, and it will details of which tool and what needs to be passed in tool
* Langchain will just give tool message, it wont directly call the tool
* We can pass the ai message and tool message to langchain to get the tool output then
*

