# Community MCP

* uvx or nvx
* We can use the json for configuring in our client
* Use the same in our langchain client
* In the below one it will be used as stdio
* It will be downloaded locally



```python
{
    "mcpServers": {
        "ddg-search": {
            "command": "uvx",
            "args": ["duckduckgo-mcp-server"]
        }
    }
}
```
