# Parameters

* Parameters are the internal values tuned during training — the model's actual settings.&#x20;
* No single parameter means anything on its own; capability emerges from the combined position of billions of them.
