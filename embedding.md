# Embedding

* Once text is broken into tokens, each one gets converted into a long list of numbers — a vector — that captures its meaning as a location in space.&#x20;
* Words that mean similar things end up near each other in that space; unrelated words end up far apart
